document.addEventListener("DOMContentLoaded", () => {
  const mount = document.getElementById("signalApp");
  if (mount) {
    mount.innerHTML = "<p><strong>Household Signal loading…</strong></p>";
  }
});
